name: authentication_repository
description: Dart package which manages the authentication domain.
version: 1.0.0

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  meta: ^1.1.8
  formz: ^0.3.1
  bloc: ^6.0.3
  flutter_bloc: ^6.0.6
  equatable: ^1.2.5
